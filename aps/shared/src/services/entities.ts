export interface UserAttrs {
  userId: string;
  email: string;
  name: string;
  surname: string;
  birthDate: string;
  identifier: string;
}

export interface AuthAttrs {
  email: string;
  password: string;
}
